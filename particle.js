let Particle = function(pos,size,dir,color,updateCallback){
  this.pos.x = pos.x;
  this.pos.y = pos.y;
  this.radius = size;
  this.dir.x = dir.x;
  this.dir.y = dir.y;
  this.color = color;
  this.life = 5;

  this.update = function(){
    
  }

  this.draw = function(){

  }
}
